import React from 'react'
import ReactDOM from 'react-dom/client'
import ETFMonitorEnhanced from './ETFMonitorEnhanced.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ETFMonitorEnhanced />
  </React.StrictMode>,
)